---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: zones.dns.appthrust.io
spec:
  group: dns.appthrust.io
  names:
    kind: Zone
    listKind: ZoneList
    plural: zones
    singular: zone
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.domainName
      name: Domain
      type: string
    - jsonPath: .spec.zoneClassRef.name
      name: Class
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Zone represents a provider hosted zone or zone.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              adoption:
                description: Adoption selects an existing external zone to manage.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              allowedRecordSets:
                description: AllowedRecordSets grants cross-namespace RecordSet attachment.
                items:
                  properties:
                    namespaces:
                      description: Namespaces selects namespaces allowed to attach
                        RecordSets.
                      properties:
                        selector:
                          description: Selector selects namespaces allowed to attach
                            RecordSets.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - selector
                      type: object
                    records:
                      description: Records limits allowed record names and types.
                      items:
                        properties:
                          name:
                            description: Name limits the relative record owner name.
                            properties:
                              pattern:
                                description: Pattern is a regular expression matched
                                  against the relative record name.
                                minLength: 1
                                type: string
                            required:
                            - pattern
                            type: object
                          types:
                            description: Types limits the record types.
                            items:
                              type: string
                            minItems: 1
                            type: array
                        required:
                        - name
                        - types
                        type: object
                      minItems: 1
                      type: array
                  required:
                  - namespaces
                  - records
                  type: object
                type: array
              domainName:
                description: DomainName is the normalized ASCII zone apex without
                  a trailing root dot.
                maxLength: 253
                pattern: ^([a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)(\.([a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?))*$
                type: string
                x-kubernetes-validations:
                - message: domainName is immutable
                  rule: self == oldSelf
              provider:
                description: Provider references a Provider version.
                properties:
                  name:
                    description: Name is the cluster-scoped Provider resource name.
                    minLength: 1
                    type: string
                  version:
                    description: Version is the Provider version name.
                    minLength: 1
                    type: string
                required:
                - name
                - version
                type: object
              zoneClassRef:
                description: ZoneClassRef selects the provider implementation and
                  parameters.
                properties:
                  name:
                    description: Name is the ZoneClass name.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace defaults to the Zone namespace when omitted.
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: zoneClassRef is immutable
                  rule: self == oldSelf
            required:
            - domainName
            - provider
            - zoneClassRef
            type: object
            x-kubernetes-validations:
            - message: provider is immutable
              rule: '!has(oldSelf.provider) || self.provider == oldSelf.provider'
          status:
            properties:
              conditions:
                description: Conditions include Accepted and Programmed.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              nameServers:
                description: NameServers contains provider-assigned authoritative
                  name servers.
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration is the Zone generation reflected by
                  this status.
                format: int64
                type: integer
              provider:
                description: Provider contains provider-specific public status.
                properties:
                  data:
                    description: Data contains public provider-specific structured
                      status.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  state:
                    description: State contains provider-controller internal state.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
