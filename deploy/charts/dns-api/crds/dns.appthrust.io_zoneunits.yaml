---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: zoneunits.dns.appthrust.io
spec:
  group: dns.appthrust.io
  names:
    kind: ZoneUnit
    listKind: ZoneUnitList
    plural: zoneunits
    singular: zoneunit
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider.name
      name: Provider
      type: string
    - jsonPath: .spec.zone.domainName
      name: Zone
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZoneUnit is the zone-scoped desired-state and ownership ledger
          built from accepted Zone and RecordSet claims.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              provider:
                description: Provider is the Provider storage version used inside
                  this ZoneUnit.
                properties:
                  name:
                    description: Name is the cluster-scoped Provider resource name.
                    minLength: 1
                    type: string
                  version:
                    description: Version is the Provider version name.
                    minLength: 1
                    type: string
                required:
                - name
                - version
                type: object
              recordSets:
                description: |-
                  RecordSets contains accepted desired state and retained cleanup ledger state
                  for owner RecordSet claims.
                items:
                  properties:
                    a:
                      description: A defines a standard A record body.
                      properties:
                        addresses:
                          description: Addresses contains IPv4 addresses.
                          items:
                            pattern: ^((25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9]?[0-9])\.){3}(25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9]?[0-9])$
                            type: string
                          minItems: 1
                          type: array
                      required:
                      - addresses
                      type: object
                    aaaa:
                      description: AAAA defines a standard AAAA record body.
                      properties:
                        addresses:
                          description: Addresses contains IPv6 addresses.
                          items:
                            type: string
                          minItems: 1
                          type: array
                      required:
                      - addresses
                      type: object
                    adoption:
                      description: Adoption contains provider-specific adoption payload
                        in storage-version form.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    allowed:
                      description: |-
                        Allowed indicates whether current composition policy allows normal provider
                        reconciliation for this item. Omitted means true. False keeps ownership and
                        cleanup state without allowing provider-side create or update.
                      type: boolean
                    caa:
                      description: CAA defines a standard CAA record body.
                      properties:
                        records:
                          description: Records contains Certification Authority Authorization
                            entries.
                          items:
                            properties:
                              flags:
                                description: Flags is the CAA flags field.
                                format: int32
                                maximum: 255
                                minimum: 0
                                type: integer
                              tag:
                                description: Tag is the lowercase ASCII CAA property
                                  tag.
                                minLength: 1
                                pattern: ^[a-z0-9]+$
                                type: string
                              value:
                                description: Value is the CAA property value.
                                minLength: 1
                                type: string
                            required:
                            - flags
                            - tag
                            - value
                            type: object
                          minItems: 1
                          type: array
                      required:
                      - records
                      type: object
                    cname:
                      description: CNAME defines a standard CNAME record body.
                      properties:
                        target:
                          description: Target contains the canonical target DNS name
                            without a trailing root dot.
                          minLength: 1
                          type: string
                      required:
                      - target
                      type: object
                    deletionRequested:
                      description: DeletionRequested asks the provider controller
                        to delete the provider-side record target.
                      type: boolean
                    mx:
                      description: MX defines a standard MX record body.
                      properties:
                        records:
                          description: Records contains mail exchange entries.
                          items:
                            properties:
                              exchange:
                                description: |-
                                  Exchange contains the canonical mail server DNS name without a trailing root dot.
                                  Use "." with preference 0 for Null MX.
                                minLength: 1
                                type: string
                              preference:
                                description: Preference is the MX priority. Lower
                                  values have higher priority.
                                format: int32
                                maximum: 65535
                                minimum: 0
                                type: integer
                            required:
                            - exchange
                            - preference
                            type: object
                          minItems: 1
                          type: array
                      required:
                      - records
                      type: object
                    name:
                      description: Name is the zone-relative DNS owner name.
                      type: string
                    ns:
                      description: NS defines a standard delegated NS record body.
                      properties:
                        nameServers:
                          description: NameServers contains delegated name server
                            DNS names without trailing root dots.
                          items:
                            type: string
                          minItems: 1
                          type: array
                      required:
                      - nameServers
                      type: object
                    observedGeneration:
                      description: ObservedGeneration is the RecordSet generation
                        used to build this item.
                      format: int64
                      type: integer
                    options:
                      description: Options contains provider-specific record options
                        in storage-version form.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    recordSetName:
                      description: RecordSetName is the owner RecordSet name.
                      type: string
                    recordSetNamespace:
                      description: RecordSetNamespace is the owner RecordSet namespace.
                      type: string
                    ttl:
                      description: TTL is the record TTL in seconds for standard records.
                      format: int32
                      type: integer
                    txt:
                      description: TXT defines a standard TXT record body.
                      properties:
                        values:
                          description: Values contains logical TXT resource record
                            values.
                          items:
                            type: string
                          minItems: 1
                          type: array
                      required:
                      - values
                      type: object
                    type:
                      description: Type is the DNS record type.
                      type: string
                  required:
                  - name
                  - recordSetName
                  - recordSetNamespace
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - recordSetNamespace
                - recordSetName
                x-kubernetes-list-type: map
              zone:
                description: Zone is the accepted desired state for the owning Zone
                  claim.
                properties:
                  adoption:
                    description: Adoption is provider-specific adoption payload in
                      storage-version form.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  domainName:
                    description: DomainName is the accepted zone domain name.
                    type: string
                  observedGeneration:
                    description: ObservedGeneration is the Zone generation used to
                      build this item.
                    format: int64
                    type: integer
                  ref:
                    description: Ref points to the owning Zone claim.
                    properties:
                      name:
                        description: Name is the object name.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is the object namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  zoneClassRef:
                    description: ZoneClassRef is the resolved ZoneClass reference.
                    properties:
                      name:
                        description: Name is the object name.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is the object namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                required:
                - domainName
                - ref
                - zoneClassRef
                type: object
            required:
            - provider
            - zone
            type: object
            x-kubernetes-validations:
            - message: provider is immutable
              rule: '!has(oldSelf.provider) || self.provider == oldSelf.provider'
          status:
            properties:
              conditions:
                description: Conditions contains resource-level conditions such as
                  Programmed.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the ZoneUnit generation observed
                  by the provider controller.
                format: int64
                type: integer
              provider:
                description: Provider contains provider-controller internal state
                  for the whole unit.
                properties:
                  data:
                    description: Data contains public provider-specific structured
                      status.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  state:
                    description: State contains provider-controller internal state.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              recordSets:
                description: RecordSets contains provider results for accepted owner
                  RecordSet claims.
                items:
                  properties:
                    conditions:
                      description: Conditions contains provider acceptance and programming
                        result for the RecordSet claim.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    deletionCompleted:
                      description: DeletionCompleted is true after provider-side record
                        cleanup has completed.
                      type: boolean
                    observedGeneration:
                      description: ObservedGeneration is the owner RecordSet generation
                        observed in ZoneUnit.spec.
                      format: int64
                      type: integer
                    provider:
                      description: Provider contains public provider data and optional
                        provider state for this record target.
                      properties:
                        data:
                          description: Data contains public provider-specific structured
                            status.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        state:
                          description: State contains provider-controller internal
                            state.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    recordSetName:
                      description: RecordSetName is the owner RecordSet name matching
                        spec.recordSets[].
                      type: string
                    recordSetNamespace:
                      description: RecordSetNamespace is the owner RecordSet namespace
                        matching spec.recordSets[].
                      type: string
                  required:
                  - recordSetName
                  - recordSetNamespace
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - recordSetNamespace
                - recordSetName
                x-kubernetes-list-type: map
              zone:
                description: Zone contains provider result for the owning Zone claim.
                properties:
                  conditions:
                    description: Conditions contains provider acceptance and programming
                      result for the Zone claim.
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                  nameServers:
                    description: NameServers contains provider-assigned authoritative
                      name servers.
                    items:
                      type: string
                    type: array
                  provider:
                    description: Provider contains public provider data and optional
                      provider state for the zone target.
                    properties:
                      data:
                        description: Data contains public provider-specific structured
                          status.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      state:
                        description: State contains provider-controller internal state.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                type: object
            type: object
        type: object
        x-kubernetes-validations:
        - message: metadata.name must match spec.zone.ref.name
          rule: self.metadata.name == self.spec.zone.ref.name
    served: true
    storage: true
    subresources:
      status: {}
