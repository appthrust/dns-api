apiVersion: v1
kind: Service
metadata:
  name: dns-api-webhook-service
  namespace: dns-api-system
  labels:
    app.kubernetes.io/name: dns-api
    app.kubernetes.io/component: webhook
spec:
  selector:
    app.kubernetes.io/name: dns-api
    app.kubernetes.io/component: controller-manager
  ports:
    - name: https
      port: 443
      targetPort: 9443
