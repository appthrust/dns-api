---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: route53identities.route53.dns.appthrust.io
spec:
  group: route53.dns.appthrust.io
  names:
    kind: Route53Identity
    listKind: Route53IdentityList
    plural: route53identities
    singular: route53identity
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.accountID
      name: Account
      type: string
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Route53Identity selects the AWS account and IAM identity used
          for Route 53.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              accountID:
                description: AccountID is the AWS account ID expected after credential
                  resolution.
                pattern: ^[0-9]{12}$
                type: string
              assumeRoleChain:
                description: AssumeRoleChain contains IAM roles assumed in order from
                  the base credential.
                items:
                  properties:
                    externalID:
                      description: ExternalID is passed to STS AssumeRole.
                      type: string
                    roleARN:
                      description: RoleARN is the IAM role ARN to assume.
                      minLength: 1
                      pattern: ^arn:aws[a-zA-Z-]*:iam::[0-9]{12}:role/[A-Za-z0-9+=,.@_/-]+$
                      type: string
                    sessionName:
                      description: SessionName is passed to STS AssumeRole. The controller
                        generates one when omitted.
                      maxLength: 64
                      type: string
                  required:
                  - roleARN
                  type: object
                type: array
              credentials:
                description: Credentials selects the base credential source.
                properties:
                  runtime:
                    description: Runtime uses the AWS SDK default credential chain
                      from the controller runtime.
                    type: object
                type: object
                x-kubernetes-validations:
                - message: credentials.runtime must be specified
                  rule: has(self.runtime)
              region:
                description: Region is the AWS region used for AWS SDK and STS endpoint
                  resolution.
                pattern: ^[a-z]{2}(-[a-z0-9]+)+-[0-9]+$
                type: string
            required:
            - accountID
            - credentials
            - region
            type: object
          status:
            properties:
              accountID:
                description: AccountID is the AWS account ID returned by STS for the
                  final credential.
                type: string
              conditions:
                description: Conditions describe whether this identity can be referenced.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastCredentialCheckTime:
                description: LastCredentialCheckTime is the last time the controller
                  tried a live AWS identity check.
                format: date-time
                type: string
              nextCredentialCheckTime:
                description: NextCredentialCheckTime is the controller-planned next
                  live AWS identity check time.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last reconciled generation.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
